## アイデンティティベース・リソースベースのポリシーをどちらに設定するかの設計思想
### リソース間連携の許可設定をどちらにするかの判断基準※送信元か宛先どちらの許可設定をするかという思想を統一するため
- 呼び出し元(プリンシパル or 主体)が誰になるかで判断
  - EC2やECSなど、自分のロールが呼ぶ → IAMに集約
  - CloudFront、ALB、Cloudwatch等のサービスプリンシパルが呼ぶ → 受け側リソースポリシー(S3のパケットポリシーなど)
  - サービスに実行ロールを渡す方式(Scheduler等)→ これもIAM側(渡すロールのポリシーと、iam:PassRoleの管理に注意)
- 例外)
  - KMSはキーポリシーが必須であり、通常はキーポリシーからIAMへ委譲した上でIAMポリシーで権限管理する
  - ガードレールとしてのDenyはリソースポリシー側に定義するのが一般的

## AWS WAFの挙動
AWS WAFはHTTP/HTTPSリクエスト(L7)を評価するサービスであり、CloudFront・ALB・API Gateway等のリクエスト処理の途中で評価される。
WAFでBlockされたリクエストはバックエンド(ECS・EC2・Lambda等)には到達しない。
- IP制限
    - WAFのIP制限はL7でHTTPリクエストを評価してBlockする
    - SGのようにL3/L4で通信自体を拒否するものではない
    - TCP接続までは確立されるため、L3/L4レベルの攻撃を防ぐことはできない
